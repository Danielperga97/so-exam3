from setuptools import setup, find_packages

setup(
    name='op_stats',
    version='0.1.0',
    packages=['A00018200.op_stats'],    
    install_requires=[
        'flask',
        'psutil'
    ]
)
